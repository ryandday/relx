# relx Documentation

The documentation index lives in [README.md](README.md).

The published site is built from `index.html` (landing page) plus Doxygen-generated API pages; see
`.github/workflows/docs.yml`.
